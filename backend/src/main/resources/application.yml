server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/money_transfer_db
    username: root
    password: Cloud@123$
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

snowflake:
  url: jdbc:snowflake://jwplyga-ye20201.snowflakecomputing.com
  user: Varsha
  password: Varshashripad@2004
  warehouse: COMPUTE_WH
  database: MONEY_TRANSFER_DW
  schema: ANALYTICS
  role: ACCOUNTADMIN

logging:
  level:
    org.hibernate.SQL: DEBUG

jwt:
  secret: supersecurekeythatislongenoughforHS256algorithm1234
  expiration: 3600000 # 1 hour in milliseconds
